# 更新日志

本项目的正式版本遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.2.1] - 2026-08-07

### 修复

- 修复 Windows PowerShell 5.1 首次探测不存在的 GitHub Release 时提前终止发布的问题。

### API、CLI 与配置兼容性

- 无用户 API、CLI 或配置变更。

## [0.2.0] - 2026-08-07

### 新增

- 增加 `s3-url` 命令，可为桶内对象生成使用公网域名的预签名 GET 下载链接。
- 增加可配置的公网 endpoint、桶名和链接有效天数。

### 变更

- 将 S3 endpoint、区域、桶名、寻址方式等非敏感配置从 `.env` 迁移至 `config/s3.yml`。
- `.env` 中的 S3 配置仅保留访问密钥与私密密钥。

### 修复

- 生成外部链接前通过内网 endpoint 检查对象是否存在，不再为不存在的对象输出链接。
- 对象不存在、无权限或连接失败时返回明确的非零退出码，且错误输出不会泄露凭据。

### API、CLI 与配置兼容性

- 新增 CLI：`s3-url --path <桶内对象 Key>`。
- `s3-upload` 的非敏感配置来源变更为 `config/s3.yml`，部署时需要完成配置迁移。
- 新增运行时依赖 `PyYAML`。
